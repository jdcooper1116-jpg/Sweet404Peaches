# Railway Postgres connection string used by Prisma.
# Keep real credentials in local/hosted environment variables only.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Storage provider flag for the gradual Firebase-to-Postgres migration.
# Keep firebase until individual routes are migrated and verified.
DREAM_DB_PROVIDER=firebase
